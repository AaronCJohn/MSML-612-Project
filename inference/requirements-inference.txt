# Inference-only deps. Install PyTorch for your platform first, then:
#   pip install -r inference/requirements-inference.txt
#
# GPU (pick one): https://pytorch.org/get-started/locally/
#   pip install torch torchvision
# Apple Silicon often works with the default CPU/MPS wheels from PyPI.

numpy>=1.20
Pillow>=9.0
torch>=2.0
torchvision>=0.15
tqdm>=4.60
